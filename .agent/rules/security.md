# Security Rules

- **No Secrets**: Do not commit secrets to the repository. Use `.env` files.
- **Sanitize Inputs**: Validate and sanitize all user inputs.
- **Prepared Statements**: Use prepared statements for all database queries to prevent SQL injection.
